name: claudart_cli
description: A Dart CLI for managing project debug and suggestion sessions via structured markdown handoffs.
version: 1.0.0

environment:
  sdk: '>=3.0.0 <5.0.0'

executables:
  claudart: claudart

dependencies:
  args: ^2.5.0
  path: ^1.8.0

dev_dependencies:
  lints: ^4.0.0
  test: ^1.24.0
  mocktail: ^1.0.0
